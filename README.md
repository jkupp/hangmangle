# Hangmangle

A two-team Hangman variant. Static site, deploys on Vercel, state in Firebase Firestore.

Setup: paste your Firebase web config into `firebase.js`, then serve the directory (any static server works).
